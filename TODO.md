## TODO-NEW
1. Add new stock
  - truncate whole stock 
  - create a new stock tick
1. insert_to_stock_table is very complex, simplify it
1. When fetch from API, 
  - add a mock for response
  - use smart way ( check date) to fetch
  - use API get limited column
1. Separate save stock to DB to a single solution
1. Do not use Json 
  - before Save to DB
  - draw png
1. Separate DB Table, if bull and more MA
1. for stockDB folder, do not include so many files
1. use docker compose for API fetch + analyze
1. add unit tests
1. China stock
- disable for now
- find China index
- enable again

### done
~1. fix to make it work~
~1. move stock list to const file~


## TODO-OLD:

1. Draw only for the stock list (get_all_tables)
2. Clean up old DB
3. fix python docker - DB backup

## TEST:

## ANALYZE:

Trade strategy
regression buy/sell

## LEARN:

decorator
timeit
Functions can be passed as arguments to another function. If you have used functions like map, filter and reduce in Python, then you already know about this.

